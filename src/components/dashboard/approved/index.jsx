import React from 'react'
import ApprovedCard from './ApprovedCard'

const Approved = () => {
  return (
    <div>
        {/* {allRequests?.map(req => (
            <ApprovedCard key={req._id} {...{req, refreash, setRefreash}}/>
        ))} */}
    </div>
  )
}

export default Approved